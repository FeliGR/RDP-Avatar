/**
 * Constants used throughout the dialog components
 */

// Custom events for typing status
export const TYPING_EVENTS = {
  START: "user-typing-start",
  STOP: "user-typing-stop",
};

// Debounce delay for typing events (in milliseconds)
export const TYPING_DEBOUNCE_DELAY = 2000;

// Status message display time (in milliseconds)
export const STATUS_MESSAGE_DURATION = 2000;
